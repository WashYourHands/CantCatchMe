{
  "devDependencies": {
    "babelify": "^6.3.0",
    "browserify": "^11.1.0",
    "watchify": "^3.4.0"
  },
  "scripts": {
    "build": "browserify src/main.js -t babelify --outfile dist/app.js",
    "watch": "watchify src/main.js -t babelify --outfile dist/app.js"
  }
}
